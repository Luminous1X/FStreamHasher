# FStreamHasher
Asks the user for credentials, uses the FStream library to create a file that contains the credentials to be used as a reference for logging in, while also hashing the users credentials with the sodium lib (argon2id), basic explanations of how each function works is also included in the source
